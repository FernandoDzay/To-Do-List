<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fiuady.a2dolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".A1_2DoList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".A2_2DoList" />
        <activity
            android:name=".A3_2DoList"
            android:label="@string/title_activity_a3_2_do_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".A4_2DoList" />
        <activity android:name=".A5_2DoList"></activity>
    </application>

</manifest>